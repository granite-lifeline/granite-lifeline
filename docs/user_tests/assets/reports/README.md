# Controlled Report Stimuli

This directory remains empty until the Report group supplies an approved
same-case PDF pair and it passes `../screenshot_capture_plan.md` §4A.

Create these two condition directories only during controlled capture:

```text
rag/page-01.png
baseline/page-01.png
```

Continue the same zero-padded sequence for every page. Store condition mapping,
source provenance, dimensions, and hashes in `../README.md`; do not place the
source PDFs or participant-identifying data here.
