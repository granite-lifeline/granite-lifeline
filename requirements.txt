# Core dependencies
pydantic>=2.0.0,<3.0.0
pandas==3.0.3
numpy==2.4.6
streamlit>=1.28.0
plotly>=5.0.0
reportlab>=4.0.0

# Model Layer TTM dependencies are intentionally not installed in the
# hosted dashboard environment. Report Layer RAG dependencies are also
# kept out of this hosted-demo file because the public dashboard loads
# static ReportLayerOutput JSON until a local CSV pipeline is configured.
# For local live CSV analysis, setup.sh / setup.ps1 install
# requirements-local.txt and model_layer/ttm-related/requirements.txt.

# Development and testing
pytest>=7.4.0
flake8>=6.1.0
